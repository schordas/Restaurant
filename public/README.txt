-Run the program
-Select a file to open.
-If the file is not an XML file, a dialog box will indicate that there is an error and you must select an XML file.
-If the file is an XML file, but is not formated correctly a dialog box will indicate that you must enter a 
properly formated XML. (An exception will appear in the console but the program will continue to run).
-All the data should appear appropriately in the GUI.
-The click table and change status functionality does not work.